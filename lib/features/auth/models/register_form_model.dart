class RegisterFormModel {
    final TextInputModel username; 
    final TextInputModel email;
    final TextInputModel password;

    RegisterFormModel({
        required this.username,
        required this.email,
        required this.password,
    });
}
